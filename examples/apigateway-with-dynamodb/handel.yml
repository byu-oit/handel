version: 1

name: apigateway-example

extensions:
  sns: sns-handel-extension

environments:
  dev:
    webapp:
      type: apigateway
      description: My Description
      swagger: ./swagger.json
      tags:
        mytag: myvalue
      dependencies:
      - topic
    topic:
      type: sns::sns
