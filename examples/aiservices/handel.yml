version: 1

name: ai-service-example

environments:
  dev:
    function:
      type: apigateway
      proxy:
        path_to_code: .
        handler: index.handler
        runtime: python3.6
      dependencies:
      - aiaccess
      - bucket
    aiaccess:
      type: aiservices
      ai_services:
      - rekognition
      - polly
      - comprehend
      - translate #translate is having an error and not deploying with the rest of the apigateway
    bucket:
      type: s3
